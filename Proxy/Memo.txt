# 定義
別のオブジェクトの代理（プレースホルダ）を提供し、
そのオブジェクトへのアクセスを制御する。

# 解釈
別プロセス（ネットワーク）にあるオブジェクトを扱う際に、
プロキシを通じてやり取りする。
複雑なアクセス制御部分はプロキシに任せる。
プロキシとやり取りする際は、最低限のインターフェースおよびシリアライズを行うことで負荷を軽くする。

# 保護プロキシ
アクセス制御を行うということは、
プロキシを動的に作成することで（これは必須か不明）、
クライアントに応じてサブジェクトへのインタフェースを変更する＝サブジェクトへのアクセス範囲を制御することができる。